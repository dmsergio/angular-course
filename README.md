## Angular CLI

```shell
ng new {projectName}  # create a new project
ng generate component {destPath/componentName}  # create a component
ng generate module {moduleName}  # create a module

ng serve  # run the app
ng serve -o  # run the app and open it in the browser
```
